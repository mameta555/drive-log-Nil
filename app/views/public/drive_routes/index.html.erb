<h2>ドライブルート投稿一覧</h2>
<div class="container">
  <div class="row">
  <% @drive_routes.each do |drive_route| %>
    <div class="col">
      <table>
        <tr>
          <th>
            <%= link_to drive_route_path(drive_route) do %>
              <%= image_tag drive_route.get_image(300,200) %><br>
              <%= drive_route.title %>
            <% end %>
          </th>
        </tr>
        <tr>
          <th>
            <%= image_tag drive_route.user.get_profile_image(30,30) %>
            <%= drive_route.user.name %>
          </th>
        </tr>
      </table>
    </div>
  <% end %>
  </div>
</div>